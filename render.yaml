services:
  - type: web
    name: notesapp
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: "gunicorn notesproject.wsgi"
    staticPublishPath: staticfiles